<div id="intro">
  <div class="item background-cover" style="background: url('../assets/home1.jpg') center center no-repeat">
    <div class="container">
      <div class="row">
        <div class="carousel-caption-center colour-white">
          <h2>Create unique online <span class="rotate handwritting">christmas , birthday , wedding , greeting </span>cards.</br>
          Sign with friends and surprise that special someone.</h2>
          <% if user_signed_in? %>
            <p><%= link_to 'Create A Free Evercard', create_path, :class => "btn btn-lg btn-primary signup" %></p>
          <% else %>
            <p><%= link_to 'Create A Free Evercard', login_path, :class => "btn btn-lg btn-primary signup" %></p>
          <% end %>
          <h4><%= link_to 'See  Nimo\'s Birthday Evercard', card_path(@cards.find(1)) %></h4>
        </div>
      </div>
    </div>
    <div class="overlay-bg"></div>
  </div>
</div>

